# Rules 元规范

**版本**: 2.0
**最后更新**: 2026-01-01
**说明**: 定义 `.claude/rules/` 目录下规则文件的统一结构、命名和格式规范

## 🎯 适用范围

本规范**仅适用于** `.claude/rules/` 目录下的规则配置文件。

**说明**：`.claude/agents/` 等其他目录下的文件有其特定的格式要求，不在本规范约束范围内。

## 📋 文件命名规范

### 核心原则

配置文件统一使用 **PascalCase** 命名，避免使用连字符（-）或下划线（_）

### 命名模式

| 类型 | 命名规则 | 示例 | 说明 |
|------|---------|------|------|
| **配置类** | `名词.md` | `AgentMapping.md`、`CaseTemplate.md` | 配置文件使用简洁名词 |
| **规范类** | `XxxRules.md` 或 `XxxStandards.md` | `TimeRules.md`、`OutputStandards.md` | 规范文件使用 Rules 或 Standards 后缀 |
| **工作流类** | `名词.md` | `Workflow.md` | 工作流配置使用简洁名词 |

### 正确示例

```bash
AgentMapping.md          # ✅ 正确
TimeRules.md             # ✅ 正确
Workflow.md              # ✅ 正确
OutputStandards.md       # ✅ 正确
DocAnalyzerWorkflow.md   # ✅ 正确
```

### 错误示例

```bash
agent-mapping.md         # ❌ 应使用 PascalCase
time_rules.md            # ❌ 不应使用下划线
workflow-system.md       # ❌ 应使用 PascalCase
```

## 📋 标准文档结构

### 1. 文档标题

```markdown
# 文档标题

**版本**: 1.0
**最后更新**: YYYY-MM-DD
**说明**: 文档说明（可选）
```

### 2. 正文内容

使用清晰的章节结构和适当的emoji图标：

```markdown
## 🎯 章节标题

内容...

### 🔸 子章节

内容...
```

### 4. 变更历史（必需）

每个文档必须以变更历史结尾：

```markdown
## 🔄 变更历史

| 版本 | 日期 | 更新内容 |
| :--- | :--- | :--- |
| v1.0 | 2026-01-01 | 初始版本 |
```

### 5. 文档页脚（可选）

```markdown
---

*本文档是XXX系统的核心文件，自动加载到项目记忆中。*
```

## 📅 变更历史规范

### 版本号规则

遵循**语义化版本（Semantic Versioning）**：

```
主版本号.次版本号.修订号
例如：1.2.3
```

- **主版本号（Major）**：不兼容的API修改或重大架构变更
- **次版本号（Minor）**：向下兼容的功能性新增
- **修订号（Patch）**：向下兼容的问题修正

### 变更历史格式

```markdown
## 🔄 变更历史

| 版本 | 日期 | 更新内容 |
| :--- | :--- | :--- |
| v1.2.0 | 2026-01-15 | 新增XXX功能，优化YYY配置 |
| v1.1.0 | 2026-01-10 | 新增XXX规则 |
| v1.0.1 | 2026-01-05 | 修复XXX问题 |
| v1.0.0 | 2026-01-01 | 初始版本 |
```

### 变更记录撰写要点

1. **使用中文**：所有更新内容使用中文描述
2. **具体明确**：清晰说明变更的内容和影响
3. **关键词优先**：使用"新增"、"优化"、"修复"、"删除"、"重构"等关键词
4. **简洁扼要**：每条记录控制在20字以内
5. **倒序排列**：最新版本在最上面
6. **头部=表顶强一致**（v1.11.1 确立）：文档头部 `**版本**` 必须等于变更历史表顶行版本号。每次修订规范文件**必须同步**：升头部版本号 + 在变更表顶部加对应行。二者不一致即为工程债，须立即对齐（不倒填虚构的逐版本明细，可用一条"与头部对齐"说明行收敛）。

## 📝 章节命名规范

| 章节标题 | 用途 | Emoji |
|---------|------|-------|
| `## 🎯 核心原则` | 核心原则或目标 | 🎯 |
| `## 📋 规范说明` | 规范或标准说明 | 📋 |
| `## 📁 目录结构` | 目录或文件结构 | 📁 |
| `## 🔤 命名规则` | 命名相关规范 | 🔤 |
| `## 📊 分类标准` | 分类或类型说明 | 📊 |
| `## 🔄 变更历史` | 变更历史记录 | 🔄 |
| `## 🚨 注意事项` | 重要提醒或警告 | 🚨 |
| `## ✅ 检查清单` | 检查清单 | ✅ |
| `## ❌ 错误示例` | 错误示范 | ❌ |
| `## 💡 最佳实践` | 最佳实践建议 | 💡 |

## 🔄 变更历史

| 版本 | 日期 | 更新内容 |
| :--- | :--- | :--- |
| v2.0.0 | 2026-01-01 | 整合命名规范与文档结构规范，统一Rules文件规范 |
| v1.1.0 | 2026-01-01 | 精简规范内容，删除Front Matter、目录结构、格式规范等冗余章节 |
| v1.0.0 | 2026-01-01 | 初始版本，定义配置文档结构规范和变更历史规则 |

---

*本文档是SuitAgent系统文档结构规范的核心文件，确保所有配置文档的一致性。*
