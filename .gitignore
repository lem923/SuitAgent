# 开发环境文件
.DS_Store
.vscode/
.idea/
*.tmp
*.swp
*.swo
*~

# 开发工具生成的临时文件
.specstory/
.history/

# VSCode 工作区配置文件（本地开发配置）
*.code-workspace
SuitAgent.code-workspace

# 日志文件
*.log


# VSCode Extension build artifacts
vscode-extension/dist/
vscode-extension/node_modules/
vscode-extension/out/
vscode-extension/.vscode-test/
vscode-extension/*.vsix

# 依赖包
node_modules/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.env
.venv
env/
venv/

# 项目文档（不纳入版本控制）
docs/
status/

# 案件目录（格式：YYMM... 当事人 案由，必定包含中文，与其他纯英文目录区分）
[0-9][0-9][0-9][0-9]*/

# 案件目录兜底（无 YYMMNN 前缀的中文案由目录，也视为案件文件夹排除）
# 常见案由后缀
*纠纷/
*纠纷案/
*案件/
*合同案/
*侵权案/
*确权案/
*执行案/
*仲裁案/
*离婚/
*离婚案/
*协议离婚/
# 兜底：任何含"诉""与""对""V."等当事人对照符号的根级目录视为案件文件夹
*诉*/
* 与 */
* 对 */
* V. */
* v. */
* v */
# 律所内部编号格式（HC###### 等）的案件文件夹兜底
HC[0-9]*/
HC-[0-9]*/
# 案件结尾常见后缀兜底
*案/
*继承纠纷/
*侵权纠纷/
*医院案/

# 案件草稿/中间产物（可能含 client identifier，禁止上传）
tmp/

# Claude Code 本地 worktree（不入仓）
.claude/worktrees/

# 输入输出目录（由 skill 运行时使用）
input/
output/

# MCP 配置文件（包含 API 密钥）
.claude/mcp.json
.claude/mcp/*/config.json

# 用户偏好文件（个人偏好信息）
.claude/preferences/

# Claude Code 自动记忆（含个人偏好与 client identifier，禁止入仓，防个人习惯泄漏）
.claude/memory/
.claude/projects/
**/memory/MEMORY.md
**/memory/feedback_*.md
**/memory/project_*.md
**/memory/user_role.md

# 子目录中的 CLAUDE.md 文件（各组件本地配置），仅保留根目录的
**/CLAUDE.md
!CLAUDE.md

# 模板文件（律所专用模板）
.claude/skills/md2word/assets/templates/剑桥颐华律所模板.docx
.claude/skills/docx/china_law_firm_template.md

# 律所专用 skill（含具体律所名/客户对接文书规则，本地保留不入仓）
cn-firm-documents/

# MinerU OCR 归档文件
.claude/skills/mineru-ocr/archive/

